import {NextFunction} from "express";
import {StorageLogRepository} from "../repositories/storagelog.repository";

export class StatisticController {

    static async newBooks(req: Request, res: Response, next: NextFunction) {
    }

}
